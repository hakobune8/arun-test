apiVersion: v1
kind: Service
metadata:
  name: {{ include "invader-game.fullname" . }}
  labels:
    {{- include "invader-game.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "invader-game.selectorLabels" . | nindent 4 }}

apiVersion: v1
kind: Service
metadata:
  name: {{ include "gravity-invaders.fullname" . }}
  labels:
    {{- include "gravity-invaders.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gravity-invaders.selectorLabels" . | nindent 4 }}
